# Life Coach 快速开始指南

## 本地测试（Windows）

### 步骤 1: 设置虚拟环境

```powershell
# 运行虚拟环境设置脚本
.\setup_venv.ps1
```

这个脚本会：
- ✅ 检查 Python 版本
- ✅ 创建虚拟环境 `venv/`
- ✅ 安装所有必需的依赖

### 步骤 2: 运行测试

```powershell
# 激活虚拟环境（如果还未激活）
.\venv\Scripts\Activate.ps1

# 运行核心功能测试
python tests\test_core.py
```

或者使用一键测试脚本：
```powershell
.\test_local.ps1
```

### 步骤 3: 启动服务

```powershell
# 在虚拟环境中启动
python main.py
```

服务启动后，访问 http://localhost:5000 查看Web界面。

### 步骤 4: 测试API（可选）

在另一个终端：
```powershell
# 激活虚拟环境
.\venv\Scripts\Activate.ps1

# 运行API测试
python tests\test_api.py
```

### 退出虚拟环境

```powershell
deactivate
```

---

## Raspberry Pi 部署

### 准备工作

1. 硬件连接：
   - 0.96inch OLED 2inch LCD HAT 扩展板
   - GPIO4 (Pin7) - K1按键（录音）
   - GPIO24 (Pin18) - K4按键（退出）
   - I2C (GPIO2/3) - OLED显示屏

2. 系统要求：
   - Raspbian OS 12 (Bookworm) ARM64
   - Python 3.11+

### 一键部署

```bash
# 上传项目到树莓派
scp -r life-coach/ pi@raspberrypi:~/

# SSH登录树莓派
ssh pi@raspberrypi

# 运行部署脚本
cd ~/life-coach
chmod +x deploy.sh
./deploy.sh
```

部署脚本会：
- 安装系统依赖
- 启用 I2C 接口
- 下载 Whisper Tiny INT4 模型
- 安装 Python 依赖
- 创建系统服务

### 启动服务

```bash
# 启动
sudo systemctl start life-coach

# 查看状态
sudo systemctl status life-coach

# 查看日志
sudo journalctl -u life-coach -f
```

### Web界面访问

浏览器访问：`http://树莓派IP:5000`

---

## 常见问题

### Q: 虚拟环境激活失败

**A:** 如果遇到 PowerShell 执行策略限制：
```powershell
Set-ExecutionPolicy -ExecutionPolicy RemoteSigned -Scope CurrentUser
```

### Q: 依赖安装失败

**A:** 确保使用 Python 3.8+：
```powershell
python --version
```

如果版本过低，请升级 Python。

### Q: 测试显示模块未找到

**A:** 确保在虚拟环境中运行：
```powershell
.\venv\Scripts\Activate.ps1
pip install -r requirements-local.txt
```

### Q: API测试显示服务未运行

**A:** 确保先启动主服务：
```powershell
# 终端1：启动服务
python main.py

# 终端2：运行测试
python tests\test_api.py
```

---

## 项目结构

```
life-coach/
├── src/                    # 源代码
│   ├── api_server.py       # Web服务器
│   ├── display_controller.py  # 显示控制
│   ├── button_handler.py   # 按键处理
│   ├── audio_recorder.py   # 音频录制
│   ├── asr_engine.py       # 语音转写
│   └── file_storage.py     # 文件存储
├── static/                 # 前端资源
│   ├── index.html
│   ├── style.css
│   └── app.js
├── tests/                  # 测试用例
│   ├── test_core.py        # 核心测试
│   └── test_api.py         # API测试
├── docs/                   # 文档
├── main.py                 # 主程序
├── requirements-local.txt  # 本地依赖
├── requirements.txt        # 完整依赖（树莓派）
├── setup_venv.ps1         # 虚拟环境设置
└── test_local.ps1         # 本地测试脚本
```

---

## 下一步

- 📖 阅读 [API文档](docs/proposals/API接口设计.md)
- 🔧 查看 [开发计划](docs/proposals/阶段0-最小MVP开发方案.md)
- 📝 参考 [PRD](docs/PRD.md) 和 [概要设计](docs/概要设计-MVP.md)
